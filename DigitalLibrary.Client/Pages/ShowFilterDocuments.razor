@page "/documents"
@using DigitalLibrary.Client.Services
@using DigitalLibrary.Shared.DTO
@using MudBlazor
@inject NavigationManager NavigationManager
@inject DocumentsServices Documents
@inject SubjectsServices SubjectServices
@inject CategoriesServices CategoriesServices
@inject AuthorsServices AuthorsServices
@inject IJSRuntime JS
@inject ISnackbar Snackbar


<div class="home-page">
    <section class="book-category">
        <div class="category-header">
            <h2 class="category-title">@pageTitle</h2>
        </div>
        <div class="book-list">
            @if (filteredDocuments != null && filteredDocuments.Any())
            {
                @foreach (var book in filteredDocuments)
                {
                    <div class="book-card" @onclick="() => GoToBookDetail(book.id)">
                        <div class="pdf-badge">PDF</div>
                        <div class="level-badge @GetAccessClass(book.accesslevel!)">
                            @book.accesslevel
                        </div>
                        <img src="@($"https://localhost:7211{book.imageurl}")" alt="@book.title" class="book-thumbnail" />
                        <div class="book-info">
                            <p class="book-title">@book.title</p>
                            <p class="book-downloads">Lượt tải: 0</p>
                            <p class="book-author">Added by @book.publisher</p>
                        </div>
                    </div>
                }
            }
            else
            {
                <p>Xin lỗi không có tài liệu bạn cần rồi!</p>
            }
        </div>
    </section>
</div>
@if (isLoginFormVisible)
{
    <DigitalLibrary.Client.Pages.LoginForm OnClose="CloseLoginForm" />
}
@code {
    private List<DocumentsDTO> filteredDocuments = new List<DocumentsDTO>();
    private List<SubjectDTO> lstSub = new List<SubjectDTO>();
    private List<AuthorsDTO> lstAu = new List<AuthorsDTO>();
    private List<CategoriesDTO> lstCate = new List<CategoriesDTO>();
    private bool isLoginFormVisible = false;

    [Parameter] [SupplyParameterFromQuery] public int? author { get; set; }
    [Parameter] [SupplyParameterFromQuery] public int? category { get; set; }
    [Parameter] [SupplyParameterFromQuery] public int? subject { get; set; }
    [Parameter] [SupplyParameterFromQuery] public string? accessLevel { get; set; }
    [Parameter] [SupplyParameterFromQuery] public string? searchName { get; set; }
    private string pageTitle = "Tất cả tài liệu";
    private bool isGroupedView = false;

    protected override async Task OnInitializedAsync()
    {
        await LoadDocumentsAsync();
        lstAu = await AuthorsServices.GetAuthorsAsync();
        lstSub = await SubjectServices.GetSubjectsAsync();
        lstCate = await CategoriesServices.GetCategoriesAsync();
    }

    protected override async Task OnParametersSetAsync()
    {
        if (author.HasValue)
        {
            var authorName = lstAu.FirstOrDefault(au => au.id == author)!.fullname;
            pageTitle = $"Sách của tác giả: {authorName}";
        }
        else if (category.HasValue)
        {
            var categoryName = lstCate.FirstOrDefault(ca => ca.id == category)!.catename;
            pageTitle = $"Thể loại: {categoryName}";
        }
        else if (subject.HasValue)
        {
            var subjectName = lstSub.FirstOrDefault(au => au.id == subject)!.subjectname;
            pageTitle = $"Chủ đề: {subjectName}";
        }
        else if (!string.IsNullOrEmpty(accessLevel))
        {
            pageTitle = $"Hạng truy cập: {accessLevel}";
        }
        else{

        }

        if (!author.HasValue && !category.HasValue && !subject.HasValue && string.IsNullOrEmpty(accessLevel) && string.IsNullOrEmpty(searchName))
        {
            pageTitle = "Tất cả tài liệu";
            isGroupedView = false;
        }
        else if (author.HasValue)
        {
            if (author.Value == 0)
            {
                pageTitle = "Tài liệu theo tác giả";
                GroupByField("author");
            }
            else
            {
                var authorName = lstAu.FirstOrDefault(au => au.id == author)!.fullname;
                pageTitle = $"Sách của tác giả: {authorName}";
                isGroupedView = false;
            }
        }
        else if (category.HasValue)
        {
            if (category.Value == 0)
            {
                pageTitle = "Tài liệu theo thể loại";
                GroupByField("category");
            }
            else
            {
                var categoryName = lstCate.FirstOrDefault(ca => ca.id == category)!.catename;
                pageTitle = $"Thể loại: {categoryName}";
                isGroupedView = false;
            }
        }
        else if (subject.HasValue)
        {
            if (subject.Value == 0)
            {
                pageTitle = "Tài liệu theo chủ đề";
                GroupByField("subject");
            }
            else
            {
                var subjectName = lstSub.FirstOrDefault(au => au.id == subject)!.subjectname;
                pageTitle = $"Chủ đề: {subjectName}";
                isGroupedView = false;
            }
        }
        else if (!string.IsNullOrEmpty(accessLevel))
        {
            if (accessLevel == "All")
            {
                pageTitle = "Tài liệu theo hạng truy cập";
                GroupByField("accessLevel");
            }
            else
            {
                pageTitle = $"Hạng truy cập: {accessLevel}";
                isGroupedView = false;
            }
        }
        else if(!string.IsNullOrEmpty(searchName)){
            pageTitle = $"Kết quả tìm kiếm:";
            isGroupedView = false;
        }
        await LoadDocumentsAsync();
    }

    private async Task LoadDocumentsAsync()
    {
        filteredDocuments = await Documents.GetDocumentNoAuthorizeAsync(subject, author, category, accessLevel, searchName) ?? new List<DocumentsDTO>();
    }

    private void GroupByField(string field){

    }

    private void ShowLoginForm() => isLoginFormVisible = true;
    private void CloseLoginForm() => isLoginFormVisible = false;

    private async Task GoToBookDetail(int bookId)
    {
        var token = await JS.InvokeAsync<string>("localStorage.getItem", "accessToken");
        if (string.IsNullOrEmpty(token))
        {
            Snackbar.Add("Bạn phải đăng nhập mới có thể xem tài liệu!", Severity.Warning);
            ShowLoginForm();
        }
        else
        {
            NavigationManager.NavigateTo($"/detaildocument/{bookId}");
        }
    }

    private string GetAccessClass(string accessLevel) => accessLevel switch
    {
        "Basic" => "access-basic",
        "Vip" => "access-vip",
        "Pro" => "access-pro",
        _ => "access-basic"
    };
}

<style>
    :root {
        --background-color: #ffffff;
        --text-color: black;
        --card-bg: #fcfcfc;
        --shadow-color: rgba(0, 0, 0, 0.1);
        --hover-shadow: rgba(0, 0, 0, 0.2);
        --pdf-badge-bg: black;
        --pdf-badge-text: white;
    }

    body {
        background-color: var(--background-color);
        color: var(--text-color);
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .category-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px;
    }

    .category-title {
        font-size: 24px;
        font-weight: bold;
        align-items: center;
        color: #000;
        margin: 0;
    }

    .see-more {
        font-size: 14px;
        color: #000;
        text-decoration: none;
        font-weight: bold;
        transition: color 0.3s;
        margin-bottom: 10px;
    }

        .see-more:hover {
            color: #0056b3;
            text-decoration: underline;
        }

    .book-category {
        margin: 10px auto;
        text-align: left;
        padding: 30px;
        min-height: 460px;
    }

    .book-subject {
        margin: 10px auto;
        text-align: left;
        padding: 30px;
    }

    .category-title {
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 20px;
        font: #000514;
    }

    .book-list {
        display: grid;
        grid-template-columns: repeat(6, 1fr); /* 6 cột */
        gap: 15px; /* Khoảng cách giữa các sách */
        justify-content: center; /* Căn giữa danh sách */
        padding: 0 20px; /* Lề ngoài cùng */
    }

    .book-card {
        background: var(--card-bg);
        box-shadow: 0px 4px 8px var(--shadow-color);
        transition: transform 0.2s ease-in-out, box-shadow 0.3s;
        border-radius: 4px;
        padding: 10px 20px 0 20px;
        width: 200px;
        height: 350px;
        position: relative;
        cursor: pointer;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        padding: 12px;
        margin: 0 10px;
    }

        .book-card:hover {
            transform: translateY(-3px);
            box-shadow: 0px 6px 12px var(--hover-shadow);
        }

    .pdf-badge {
        position: absolute;
        top: 5px;
        left: 5px;
        background: var(--pdf-badge-bg);
        color: var(--pdf-badge-text);
        padding: 3px 6px;
        font-size: 12px;
        font-weight: bold;
        border-radius: 3px;
    }

    .level-badge {
        position: absolute;
        top: 5px;
        left: 5px;
        background: var(--pdf-badge-bg);
        color: var(--pdf-badge-text);
        padding: 3px 6px;
        font-size: 12px;
        font-weight: bold;
        border-radius: 3px;
        margin-left: 40px
    }

    .access-basic {
        background: #27ae60;
        color: white;
    }

    /* VIP */
    .access-vip {
        background: #f1c40f;
        color: black;
    }

    /* Pro */
    .access-pro {
        background: #c0392b;
        color: white;
    }

    .book-thumbnail {
        width: 100%;
        height: 220px;
        object-fit: cover;
        border-radius: 5px;
    }

    .book-title {
        font-size: 14px;
        font-weight: bold;
        color: black;
        font-size: 16px;
        font-weight: bold;
        margin-bottom: 10px;
        display: -webkit-box; /* Hiển thị dạng box flex */
        -webkit-box-orient: vertical; /* Căn dọc */
        -webkit-line-clamp: 2; /* Giới hạn tối đa 2 dòng */
        overflow: hidden; /* Ẩn phần bị tràn */
        text-overflow: ellipsis; /* Hiển thị dấu "..." nếu vượt quá 2 dòng */
        max-height: 2.5em; /* Đảm bảo chiều cao phù hợp (khoảng 2 dòng) */
        min-height: 2.5em;
        line-height: 1.3em;
    }

    .book-info {
        display: flex;
        flex-direction: column;
        flex-grow: 1; /* Chiếm hết không gian còn lại */
        padding: 10px;
    }

    .book-downloads {
        font-size: 13px;
        color: #555;
        font-style: italic;
        margin-bottom: 5px;
    }

    .book-author {
        margin-top: auto; /* Đẩy xuống dưới cùng */
        font-size: 12px;
        color: black;
    }
</style>